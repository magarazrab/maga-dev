export type AiTool = 'autoCaptions' | 'backgroundRemoval' | 'smartCut' | 'silenceRemoval' | 'autoReframe';
export async function requestAiJob(tool: AiTool, projectId: string, mediaUrl: string): Promise<{ jobId: string }> { const baseUrl = process.env.AI_API_BASE_URL; if (!baseUrl) throw new Error('AI service is not configured'); const response = await fetch(`${baseUrl}/jobs`, { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ tool, projectId, mediaUrl }) }); if (!response.ok) throw new Error('AI job request failed'); return response.json() as Promise<{ jobId: string }>; }
